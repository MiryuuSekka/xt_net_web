@using EpamTask10.Models;
@{
    Layout = null;
    var User = WebData.GetAllWebUsers().FirstOrDefault();
    WebData.SelectUser(User.Username, User.Password);
    FormsAuthentication.SignOut();

    Response.Redirect("~/Pages/Index.cshtml");
}